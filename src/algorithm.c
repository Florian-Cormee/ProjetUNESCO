#include<stdio.h>
#include<stdlib.h>
#include "utils.h"
#include "algorithm.h"

LDC Algo_ppv(double LAT, double LONG) {
    LDC *ldc = LDC_nouveau();
    
}

LDC* Algo_cdpi(double LATo, double LONGo, Site** s_tab, int n) {
    LDC *ldc = LDC_nouveau();
    unsigned long R0 = (MAX_TIME-BREAK_TIME)*(VITESSE/2);
    for(int i=0; i<n; i++)
    {
        if()
    }
}
